<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Weather forecast{% endblock %}</title>
        {# Run `composer require symfony/webpack-encore-bundle`
           and uncomment the following Encore helpers to start using Symfony UX #}
        {% block stylesheets %}
            {#{{ encore_entry_link_tags('app') }}#}
        {% endblock %}

        {% block javascripts %}
            {#{{ encore_entry_script_tags('app') }}#}
        {% endblock %}
    </head>
    <body>
    <ul>
        <li>
            <a href="{{ path('weather_forecast') }}">
                Weather forecast
            </a>
        </li>
        <li>
            <a href="{{ path('api_call_history') }}">
                Api call history
            </a>
        </li>
    </ul>
        {% block body %}{% endblock %}
    </body>
</html>
